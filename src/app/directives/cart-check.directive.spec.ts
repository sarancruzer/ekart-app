import { CartCheckDirective } from './cart-check.directive';

describe('CartCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new CartCheckDirective();
    expect(directive).toBeTruthy();
  });
});
